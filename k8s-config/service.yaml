apiVersion: v1
kind: Service
metadata:
  name: trade-engine-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: trade-engine
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080 # We use this specific port to access it externally
