apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-sm
spec:
  provider:
    gcpsm:
      projectID: abstract-arc-480317-s4
      auth:
        secretRef:
          secretAccessKeySecretRef:
            name: gcp-sa-key
            key: key.json
            namespace: trade-engine
